CREATE TABLE LANDING.CRAMERQA.CDC_GG_TPAIS (
  COD_PAIS NUMBER(2,0) NOT NULL,
  NOM_PAIS CHAR(20) NOT NULL,
  IND_ESTADO CHAR(1) NOT NULL,
  COD_USUARI CHAR(10) NOT NULL,
  FEC_AUDITA DATE NOT NULL,
  VAL_INTMUES NUMBER(6,2),
  COD_FINANCIAL VARCHAR2(2),
  IND_CORPORATIVO VARCHAR2(1),
  COD_PAIS_EBS NUMBER(4,0),
  COD_PAIS_ADUANA NUMBER(3,0),
  IND_CORPORATIVO_SYFCL VARCHAR2(1),
  uuid_nifi VARCHAR(36) NOT NULL,
  kafka_offset BIGINT,
  tipo VARCHAR(6),
  ts_ms TIMESTAMP_LTZ,
  op CHAR(1),
  procesado BOOLEAN DEFAULT FALSE,
  PRIMARY KEY (COD_PAIS, uuid_nifi, tipo)
);

CREATE TABLE LANDING.CRAMERQA.GG_TPAIS (
  COD_PAIS NUMBER(2,0) NOT NULL,
  NOM_PAIS CHAR(20) NOT NULL,
  IND_ESTADO CHAR(1) NOT NULL,
  COD_USUARI CHAR(10) NOT NULL,
  FEC_AUDITA DATE NOT NULL,
  VAL_INTMUES NUMBER(6,2),
  COD_FINANCIAL VARCHAR2(2),
  IND_CORPORATIVO VARCHAR2(1),
  COD_PAIS_EBS NUMBER(4,0),
  COD_PAIS_ADUANA NUMBER(3,0),
  IND_CORPORATIVO_SYFCL VARCHAR2(1),
  uuid_nifi VARCHAR(36) NOT NULL,
  kafka_offset BIGINT,
  tipo VARCHAR(6),
  ts_ms TIMESTAMP_LTZ,
  op CHAR(1),
  PRIMARY KEY (COD_PAIS, uuid_nifi, tipo)
);