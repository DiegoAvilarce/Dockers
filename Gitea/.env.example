# Archivo de ejemplo para variables de entorno de Gitea
# Copia este archivo como .env y modifica los valores según tu configuración

# ===========================================
# CONFIGURACIÓN DE USUARIO Y PERMISOS
# ===========================================

# ID de usuario del sistema (usa 'id -u' en Linux/Mac o 'wsl id -u' en Windows)
USER_UID=1000

# ID de grupo del sistema (usa 'id -g' en Linux/Mac o 'wsl id -g' en Windows)
USER_GID=1000

# ===========================================
# CONFIGURACIÓN DEL SERVIDOR
# ===========================================

# Dominio donde se ejecutará Gitea (sin protocolo)
GITEA_DOMAIN=localhost

# URL raíz completa para acceder a Gitea (incluye protocolo y puerto)
GITEA_ROOT_URL=http://localhost:3000

# Dominio para conexiones SSH (generalmente el mismo que GITEA_DOMAIN)
GITEA_SSH_DOMAIN=localhost

# ===========================================
# CONFIGURACIÓN AVANZADA (OPCIONAL)
# ===========================================

# Puerto SSH interno (normalmente 22, mapeado al 222 externamente)
# GITEA__server__SSH_PORT=22

# Configuración de base de datos (si usas una externa)
# GITEA__database__DB_TYPE=mysql
# GITEA__database__HOST=db:3306
# GITEA__database__NAME=gitea
# GITEA__database__USER=gitea
# GITEA__database__PASSWD=gitea

# Configuración de correo (para notificaciones)
# GITEA__mailer__ENABLED=true
# GITEA__mailer__HOST=smtp.gmail.com:587
# GITEA__mailer__FROM=noreply@tu-dominio.com
# GITEA__mailer__USER=tu-email@gmail.com
# GITEA__mailer__PASSWD=tu-password-de-app

# Configuración de seguridad
# GITEA__security__INSTALL_LOCK=true
# GITEA__security__SECRET_KEY=tu-clave-secreta-muy-larga

# ===========================================
# NOTAS DE CONFIGURACIÓN
# ===========================================

# Para obtener tu UID y GID:
# - Linux/Mac: ejecuta 'id -u' y 'id -g'
# - Windows con WSL: ejecuta 'wsl id -u' y 'wsl id -g'
# - Windows sin WSL: usa 1000 para ambos valores

# Para producción, considera:
# - Cambiar GITEA_DOMAIN por tu dominio real
# - Configurar HTTPS en GITEA_ROOT_URL
# - Usar una base de datos externa
# - Configurar un servidor de correo
# - Establecer claves secretas únicas

# Ejemplo para producción:
# GITEA_DOMAIN=git.mi-empresa.com
# GITEA_ROOT_URL=https://git.mi-empresa.com
# GITEA_SSH_DOMAIN=git.mi-empresa.com